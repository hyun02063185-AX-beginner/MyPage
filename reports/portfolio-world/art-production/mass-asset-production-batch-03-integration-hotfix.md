# Batch 03 Integration Hotfix

`READY_FOR_BATCH_03_SHORT_RECHECK`

The six reviewed material overlaps were removed by coordinate-only placement changes. Buoy is now the explicit `buoy` semantic type and validation requires water containment. Gangplank is explicit visual-only `gangplank`. `WORLD_LAYOUT` resolves assigned static vessel positions from `BERTHING_SLOTS`; no dynamic berth logic exists. Collision, routes, IA, depth constants, asset bytes, and BASE_URL behavior remain unchanged. Production preload remains 691,006 bytes.
