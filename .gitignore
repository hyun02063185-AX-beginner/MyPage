# 원본 PNG/JPG는 커밋하지 않는다. 웹용 WebP만 배포한다.
images/*.png
images/*.jpg

# Claude Code 세션 락 파일
.claude/*.lock

# Portfolio World local runtime files
portfolio-world/node_modules/
portfolio-world/dist/
portfolio-world/.vite/
.portfolio-work-context.local.json
.DS_Store

# Portfolio World Rebuild — materialized agent skills are generated copies.
# Source of truth is tools/agent-skills/{vendor,profiles}/; do not track the
# copies sync-skills.mjs writes here, to avoid two committed copies drifting.
.claude/skills/
.codex/skills/
