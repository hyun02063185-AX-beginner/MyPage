# 강사 페이지 스펙

MyPage 하위 페이지 2호. `SPEC.md`의 원칙과 규칙을 그대로 따르되,
이 문서가 강사 페이지에 한해 우선한다.

- 경로: `/teaching.html`
- 홈에서 링크로 연결 (홈 teaching 섹션 하단)

---

## 1. 이 페이지가 답해야 할 질문

방문자는 교육 담당자다. 그가 알고 싶은 것은 세 가지다.

1. 이 사람은 무엇을 가르치는가
2. 우리 기관에 맞는가
3. 몇 시간짜리로 뭘 받을 수 있는가

**실적을 자랑하는 페이지가 아니다.** 강의 이력은 중고등학교 출강 중심이라
규모가 크지 않다. 대신 준비된 커리큘럼과 판단 기준을 보여준다.

### 실적과 제안을 구분한다

| 구분 | 내용 | 표기 |
|---|---|---|
| 해온 것 | 출강 주제, 자격·수료 | 사실 그대로 |
| **할 수 있는 것** | 커리큘럼, 운영 구성 | **제안으로 명시** |

커리큘럼은 아직 운영해본 적이 없다.
`이런 구성으로 진행할 수 있습니다` 로 쓴다.
`이렇게 운영해왔습니다` 로 쓰면 나중에 문의 과정에서 드러난다.

---

## 2. 섹션 구성

| # | 섹션 | 내용 |
|---|---|---|
| 1 | 관점 | 관통 메시지 2개 |
| 2 | 커리큘럼 | 8모듈을 6단계 여정으로 |
| 3 | 조직 유형 | 중소기업 · 대기업 · 공공기관 관점 |
| 4 | 운영 구성 | 시간별 구성 + 선택 가이드 |
| 5 | 실습 | 4종 예시 |
| 6 | 자격·수료 | 홈에서 이관 |
| 7 | 강의 주제 | 지금까지 해온 강의 (주제만, 학교명·횟수 없음) |
| 8 | 문의 | 가능 대상·형식 안내 + 홈 contact 링크 |

6·7번은 홈에 이미 있다. 이 페이지로 옮기고 홈에서는 요약만 남긴다.
그 처리는 별도 작업으로 하고, 이번에는 이 페이지에 넣는 것까지만 한다.

---

## 3. 관점 섹션

원고 그대로 쓴다.

> 최신 기술이라서가 아니라, 방향이 맞아서 씁니다.

> 결심은 사람이, 실행은 AI가. 단, 시킬 일을 잘 골라야 합니다.

홈 explain 섹션의 대표 이미지(`kyulshimhandang.webp`)가 두 번째 메시지를
시각화한 것이다. 이 페이지에서 그 연결을 한 줄로 밝힌다.

> 홈에 걸린 좌우 대비 그림은 이 문장을 그린 것입니다.

이미지 자체는 이 페이지에 다시 넣지 않는다. 홈으로 링크만 건다.

---

## 4. 커리큘럼 섹션 — 6단계 여정

모듈 목록을 나열하지 않는다. **여정으로 보여준다.**
왜 → 무엇으로 → 어디에 → 어떻게 → 조심할 것 → 그다음.

| 단계 | 모듈 | 핵심 질문 | 학습 목표 |
|---|---|---|---|
| 왜 | ① AX란 무엇인가 | AX는 왜 지금 필요한가 | AX의 정의를 명확히 하고, 왜 지금 논의되는지 이해합니다 |
| 무엇으로 | ② AI 기본 이해 | AI는 무엇을 할 수 있고 없나 | AI가 잘하는 것과 못하는 것을 현실적으로 구분합니다 |
| | ③ RAG · 에이전트 | 조직 지식을 어떻게 AI에 연결하나 | 조직의 지식을 AI가 활용하게 만드는 방법을 이해합니다 |
| | ④ AX와 DX의 관계 | DX 없이 AX가 가능한가 | AX가 DX 위에 선다는 것을 이해하고, DX가 미비할 때의 대응을 압니다 |
| 어디에 | ⑤ 조직과 암묵지 | 우리 조직의 무엇을 AI로 옮기나 | 조직 지식을 형식지와 암묵지로 구분하고, 무엇을 옮길지 판단합니다 |
| | ⑥ 적용 대상 설계 | 워크플로우·보고라인 어디에 넣나 | 업무 흐름의 어느 지점에 AI를 넣을지 판단하는 기준을 갖습니다 |
| 어떻게 | ⑦ 성공·실패 요건과 실행 | 어떻게 시작하고 정착시키나 | 성공하는 조건과 실패하는 패턴을 알고, 현실적 실행 순서를 갖습니다 |
| 조심·그다음 | ⑧ 리스크·거버넌스·문화 | 무엇을 지키고 어떻게 문화로 만드나 | 리스크를 관리하고 일회성이 아닌 문화로 정착시키는 법을 압니다 |

각 모듈에는 **학습 목표 한 줄**만 붙인다.
모듈 상세 내용은 넣지 않는다. 페이지가 강의 자료가 되면 안 된다.

### 강조할 것
6단계가 도구 나열이 아니라 **사람이 이해하는 순서**라는 점을
섹션 도입부 한 문장으로 밝힌다.

---

## 5. 조직 유형 섹션

커리큘럼의 모든 모듈에 조직 유형별 관점이 붙어 있다.
그 중 **동기 부분만** 뽑아 세 줄로 정리한다.

| 유형 | 동기 |
|---|---|
| 중소기업 | 적은 인력으로 더 많은 일 — 생존형 효율화 |
| 대기업 | 부서 간 사일로, 축적된 지식의 활용 — 규모의 관리 |
| 공공기관 | 대민 서비스 품질, 반복 행정 경감 — 공공성과 효율의 균형 |

담당자가 자기 기관을 여기서 찾으면 나머지를 읽는다.

---

## 6. 운영 구성 섹션 — 이 페이지의 핵심

시간이 아니라 **그 시간에 무엇이 나오는지**로 설명한다.

| 구성 | 시간 | 끝난 뒤 |
|---|---|---|
| 특강 | 2~3시간 | AX가 무엇인지 이해하고, 우리 일에 맞는지 판단할 수 있다 |
| 반일 워크숍 | 4시간 | 특강 + 우리 조직 워크플로우 진단 실습 |
| 단기 과정 | 7시간 | 핵심 문서 하나를 완성한다 |
| | 14시간 | 여러 자료 유형을 반복하고 검토까지 한다 |
| | 21시간 | 업무 적용 발표까지 간다 |
| 시리즈 | 8주 | 도입·정착·거버넌스까지 실행 로드맵을 갖는다 |

### 선택 가이드

담당자가 문의 전에 스스로 고를 수 있게 한다.

| 상황 | 추천 |
|---|---|
| AX를 처음 소개, 인식 제고 | 특강 |
| 경영진 대상 짧은 브리핑 | 특강 (왜 · 조심할 것 중심) |
| 실무자가 실제 업무에 적용 | 반일 워크숍 |
| 업무 산출물까지 만들어야 함 | 7 · 14 · 21시간 |
| 조직 차원의 도입·정착 준비 | 8주 시리즈 |

**이 표가 이 페이지에서 가장 실용적인 부분이다.** 눈에 띄게 배치한다.

---

## 7. 실습 섹션

원고에 있는 4종을 그대로 쓴다.

- 우리 조직의 AX 오해 브레인스토밍
- 우리 팀의 암묵지 3가지 적어보기
- 내 업무 워크플로우를 단계로 쪼개고 AI 적합 지점 표시하기
- 우리 조직 AX 실패 시나리오 상상하기

짧은 목록으로 둔다. 설명을 붙이지 않는다.

---

## 8. 문의 섹션

담당자가 실제로 묻는 것을 미리 답한다.

- 가능 대상: 대학생 이상 성인
- 가능 형식: 대면
- 지역: 전국 (특정 지역으로 한정하지 않는다)
- 문의: 받고 있음 → 홈 contact 폼으로 링크

지역은 "서울·경기" 같은 범위로 좁히지 않는다.
조건이 맞으면 지방도 가능하므로, 범위를 적어 스스로 기회를 줄이지 않는다.

---

## 8-1. 대상 불일치 처리 — 반드시 지킬 것

문의 섹션의 가능 대상은 **대학생 이상 성인**인데,
7번 강의 주제 섹션에 나오는 출강 이력은 **중·고등학생 대상**이다.
아무 설명 없이 두면 읽는 사람에게 모순으로 보인다.

7번 섹션 제목을 **"지금까지 해온 강의"** 로 명시하고,
도입부에 한 줄을 붙여 시점 차이를 밝힌다.

> 중·고등학생을 대상으로 AI 활용 코딩과 진로 특강을 해왔고,
> 지금은 성인 대상 AX 교육으로 옮겨가고 있습니다.

이력을 감추지 않는다. 다른 분야 청중을 가르쳐본 경험은 약점이 아니다.

---

## 9. 금지

- 커리큘럼 부록의 **통계 수치**를 쓰지 않는다.
  (AX 성숙도 3단계 비율, 기대 대비 달성률 등 — 외부 자료 인용)
- 커리큘럼 부록의 **기업 사례**를 쓰지 않는다.
  (앨리 · 헨켈 · 허스크바나 · 가우스 · P-GPT 등 — 외부 자료 인용)
- 팀스파르타 트렌드 리포트, AX 강사 육성 표준개요서의 내용을 쓰지 않는다.
  **본인이 만든 것이 아니다.**
- 출강 학교 실명, 출강 횟수·건수를 쓰지 않는다.
- 커리큘럼을 운영 실적처럼 서술하지 않는다.

---

## 10. 인터랙션

- 6단계 여정은 스크롤에 따라 단계가 순서대로 나타난다.
  기존 `js/motion/reveal.js` 재사용.
- 클릭해야 내용이 나오는 구조를 만들지 않는다.
- 커리어 페이지의 sticky 레일은 여기서 쓰지 않는다.
  이 페이지는 시간 순이 아니라 구조를 보여주는 페이지다.

---

## 11. 데이터 스키마

`js/data/teaching-page.js`

기존 `js/data/teaching.js`(홈의 강의 주제)와 파일명이 겹치지 않게 한다.

```js
export const teachingPage = {
  statements: ["...", "..."],
  statementNote: "홈에 걸린 좌우 대비 그림은 이 문장을 그린 것입니다.",
  journey: [
    {
      stage: "왜",
      modules: [
        { no: "①", title: "AX란 무엇인가", question: "...", goal: "..." }
      ]
    }
  ],
  orgTypes: [
    { type: "중소기업", motive: "..." }
  ],
  formats: [
    { name: "특강", duration: "2~3시간", outcome: "..." }
  ],
  guide: [
    { situation: "...", recommend: "..." }
  ],
  exercises: ["...", "..."],
  contact: null
};
```

`contact` 는 확정 전까지 `null`. 렌더러는 `null` 이면 섹션을 그리지 않는다.

---

## 12. 파일 경계

| 에이전트 | 담당 |
|---|---|
| 콘텐츠 | `js/data/teaching-page.js` |
| 구조·스타일 | `teaching.html`, `css/teaching.css` |
| 인터랙션 | `js/motion/` (reveal 선택자 추가만) |
| 데이터·API | `js/render/teaching-page.js` |
| 오케스트레이터 | `js/teaching-main.js`, SPEC 문서 |

`index.html`, `career.html`, `css/sections.css`, `css/career.css` 는
건드리지 않는다. `css/tokens.css` 변수는 그대로 재사용한다.
