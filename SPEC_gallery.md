# 갤러리 페이지 스펙

MyPage 하위 페이지 3호. `SPEC.md`의 원칙과 규칙을 그대로 따르되,
이 문서가 갤러리 페이지에 한해 우선한다.

- 경로: `/gallery.html`
- 네비게이션에 "갤러리" 링크 추가 (커리어·강사와 나란히)

---

## 1. 이 페이지의 성격

**성과를 자랑하는 페이지가 아니다.**

"이런 자료를 교육에 썼더니 효과적이었다"를 보여주는 자리다.
방문자는 교육 담당자이고, 그가 판단하려는 것은
"이 사람이 어려운 것을 쉽게 만들 수 있는가"이다.

### 프롬프트는 공개하지 않는다
이전 논의에서 프롬프트 공개를 검토했으나 **하지 않기로 했다.**
프롬프트 공개는 "이렇게 만듭니다"이고, 이 페이지는 "이렇게 가르칩니다"다.
목적이 다른 것을 한 페이지에 섞으면 초점이 흐려진다.

데이터 스키마에 `prompt` 필드를 만들지 마라.

### 도입 문장
```
개발 용어에서 막히는 분들이 많았습니다.
그림으로 설명해보니 반응이 달랐습니다.
```

---

## 2. 분류

**대상자가 그 말을 어디서 만나는지**로 나눈다.
난이도나 기술 계층으로 나누면 개발자 관점이 된다.
이 사이트의 강의 대상은 개발자가 아니라, AI를 활용해 AX를 진행하려는
일반 운영자·업무 담당자다.

### 묶음 1 · AI가 무엇인지 알아야 할 때
| 항목 | slug | 한 줄 |
|---|---|---|
| 머신 | machine | 계산을 수행하는 장치입니다 |
| 모델 | model | 데이터에서 규칙을 찾아 담아둔 결과물입니다 |
| LLM | llm | 말을 다루도록 아주 크게 학습시킨 모델입니다 |
| 엔진 | engine | 실제로 일을 돌리는 부분입니다 |

### 묶음 2 · AI를 직접 쓸 때 만나는 말
| 항목 | slug | 한 줄 |
|---|---|---|
| 프롬프트 | prompt | AI에게 건네는 지시입니다 |
| 임베딩 | embedding | 뜻이 비슷한 것끼리 가까이 놓는 방식입니다 |

### 묶음 3 · IT 담당자와 이야기할 때 나오는 말
| 항목 | slug | 한 줄 |
|---|---|---|
| API | (홈 explain 의 제주 사례) | 필요한 것을 그때그때 가져다 쓰는 통로입니다 |
| 알고리즘 | (홈 explain 사례) | 일을 처리하는 순서입니다 |
| 클라우드 | cloud | 남의 컴퓨터를 빌려 쓰는 것입니다 |
| 터미널 | terminal-1 | 마우스 대신 글자로 컴퓨터에 시키는 창입니다 |
| 도커 | legacy-ai-webtoon-02 | 실행 환경을 통째로 담아 옮기는 방법입니다 |
| 추상화 | legacy-ai-webtoon-01 | 전부 알 필요 없이 지금 필요한 만큼만 보는 것입니다 |

총 12편.

### 주의
- `legacy-ai-webtoon-01` / `02` 는 slug 가 내용을 나타내지 않는다.
  화면에 보이는 제목은 **추상화 / 도커** 로 표기한다. slug 는 바꾸지 마라
  (이미지 파일명과 연결돼 있다)
- 위 "한 줄"은 목록에서 보이는 요약이다.
  각 항목의 상세 해설은 `js/data/webtoon-imports.js` 의 `paragraphs` 를
  그대로 쓴다. **새로 지어내지 마라.** 이미지에 실제로 적힌 내용이다
- 묶음 2가 두 편뿐이라 다른 묶음보다 짧다. 그대로 둔다.
  앞으로 웹툰이 추가되면 채워진다

---

## 3. 화면 구성

### 목록
- 묶음 3개를 순서대로. 각 묶음에 제목과 짧은 설명
- 항목은 썸네일 + 제목 + 한 줄 요약
- 웹툰은 정사각이다. `object-fit: cover` 로 자르지 마라.
  홈 explain 의 `.explain-case-card__image` 처리 방식을 참고한다

### 모달
클릭하면 화면을 어둡게 덮고 원본을 크게 띄운다.
4컷 웹툰은 크게 봐야 글자가 읽힌다.

**필수 사양**
- 배경을 어둡게 덮는다. 사유의 방 톤과 이어진다
- 이미지 아래에 제목과 해설(`paragraphs`)
- **이전 · 다음 이동** — 하나씩 닫았다 여는 것은 번거롭다.
  묶음 경계를 넘어 전체 12편을 순환한다
- **키보드** — `Esc` 닫기, `←` `→` 이동
- **배경 클릭**으로 닫기
- **모바일 스와이프**로 좌우 이동
- 모달이 열려 있는 동안 **뒤 배경 스크롤을 잠근다**
- **포커스 관리** — 열리면 모달 안으로 포커스를 옮기고,
  닫히면 원래 눌렀던 항목으로 되돌린다. 모달 밖으로 탭이 새 나가지 않게 한다

**주의**
- 모달 열기·닫기 애니메이션은 짧게(200~300ms). 페이지 전환과 다르다.
  여기서 길면 반복 조작이 답답해진다
- `prefers-reduced-motion` 에서는 애니메이션 없이 즉시 표시
- 모달은 `js/motion/pageTransition.js` 와 무관하다. 링크 이동이 아니다

---

## 4. 홈 explain 축소

현재 `js/data/explain.js` 가 `js/data/webtoon-imports.js` 를 import 해서
홈 explain 섹션에 사례 12개가 모두 나온다.

**갤러리가 생기면 홈은 2개로 줄인다.**
- 홈: 제주 동문시장 API 비유, 알고리즘 — 2개
- 갤러리: 12편 전부

### 할 일
- `explain.js` 에서 `importedWebtoonCases` 스프레드와 import 문을 제거한다
- `webtoon-imports.js` 파일 자체는 지우지 마라. 갤러리가 쓴다
- 홈 explain 섹션 하단에 갤러리로 가는 링크를 추가한다.
  문구: `설명 자료 더 보기`
  스토리 끝의 커리어 링크와 같은 방식으로, 버튼이 아니라 조용한 링크

---

## 5. 톤

- 기존 앰버 팔레트와 다크 기본 토큰을 그대로 쓴다. 새 색을 만들지 마라
- 커리어 페이지의 **스크롤 스포트라이트를 갤러리에도 적용할지**는
  구현 후 판단한다. 우선 적용하지 말고, 목록이 어떻게 보이는지 먼저 확인한다
- 등장 애니메이션은 기존 `js/motion/reveal.js` 를 재사용한다

---

## 6. 데이터 스키마

`js/data/gallery.js`

```js
export const gallery = {
  intro: "개발 용어에서 막히는 분들이 많았습니다. 그림으로 설명해보니 반응이 달랐습니다.",
  groups: [
    {
      title: "AI가 무엇인지 알아야 할 때",
      description: "...",
      items: [
        { slug: "machine", title: "머신", summary: "계산을 수행하는 장치입니다" }
      ]
    }
  ]
};
```

- 이미지 경로와 `imageAlt`, 상세 해설(`paragraphs`)은
  `js/data/webtoon-imports.js` 와 `js/data/explain.js` 에서 slug 로 참조한다.
  **내용을 복사하지 말고 import 로 연결한다.** 한쪽만 고쳐도 맞도록
- `prompt` 필드를 만들지 마라

---

## 7. 파일 경계

| 에이전트 | 담당 |
|---|---|
| 콘텐츠 | `js/data/gallery.js`, `js/data/explain.js` 수정 |
| 구조·스타일 | `gallery.html`, `css/gallery.css`, 세 페이지 네비 링크 추가 |
| 인터랙션 | `js/motion/` 에 모달 모듈 신규 |
| 데이터·API | `js/render/gallery.js` |
| 오케스트레이터 | `js/gallery-main.js`, SPEC 문서 |

`css/tokens.css`, `css/career.css`, `css/teaching.css` 는 건드리지 않는다.

---

## 8. 완료 기준

코드만 보지 말고 **실제 브라우저에서 확인한다.**

- [ ] 묶음 3개, 항목 12개가 모두 렌더링되는가
- [ ] 썸네일이 잘리지 않는가
- [ ] 클릭 시 모달이 열리고 원본이 크게 보이는가
- [ ] `Esc`, 배경 클릭, 닫기 버튼으로 닫히는가
- [ ] `←` `→` 로 12편을 순환할 수 있는가
- [ ] 모달이 열린 동안 뒤 배경이 스크롤되지 않는가
- [ ] 닫으면 원래 눌렀던 항목으로 포커스가 돌아오는가
- [ ] 375px 에서 스와이프가 동작하고 글자가 읽히는가
- [ ] 홈 explain 이 2개로 줄었는가
- [ ] 375 / 768 / 1024 / 1280px 에서 가로 스크롤이 없는가
- [ ] 다크·라이트 양쪽에서 대비가 유지되는가
- [ ] 콘솔 에러가 없는가
